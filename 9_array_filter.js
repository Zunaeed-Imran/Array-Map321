// simple array filter xample
// only show number in array => 10, all below number won't show.


let arr = [8, 9, 5, 88, 36];

const output = arr.filter(xample => xample > 10);

document.write(output);
