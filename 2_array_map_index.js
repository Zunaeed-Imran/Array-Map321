const values = [12, 9, 36, 55, 2]

const newValues = values.map((value, index) => {
    newValue = value + 2
    document.write( 'new value: ' + newValue + ' ,index number: ' + (index + 1)) // index will পর্যায়ক্রমিকভাবে নাম্বারিং করবে
})